#!/bin/sh

SCRIPT_PATH=$(realpath $0)
BIN_DIR=$(dirname ${SCRIPT_PATH})
LIB_DIR=$(realpath "${BIN_DIR}"/../lib)
APP_NAME=$(basename -s .sh $0)

export LD_LIBRARY_PATH="${LD_LIBRARY_PATH}":"${LIB_DIR}":"${LIB_DIR}64":"@QT_LIBRARIES_DIR@"

exec "${BIN_DIR}"/"${APP_NAME}" "${@}"
